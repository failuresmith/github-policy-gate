policies:
  - id: release-label-example
    severity: warn
    when:
      changed:
        - 'api/public/**'
    require:
      any:
        - has_label:
            - 'release-note-exempt'
        - changed:
            - 'CHANGELOG.md'
    message: 'Public API changes should include release-note evidence.'
